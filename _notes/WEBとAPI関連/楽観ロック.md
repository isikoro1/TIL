## 楽観ロック
排他制御の代表的な方式の一つ
### 排他制御とは
共有資源（ファイル）へのアクセスなどで競合が起こった時に、
同時書き込みによるデータの不整合などを防ぐために、
あるトランザクションがアクセス（編集）している時に他のトランザクションがアクセス（編集）
できないようにする仕組みの一つ。
他を排除する制御だから排他制御。


### 楽観ロックの仕組み
データそのものはロックしない（つまり閲覧やアクセス自体はできる）

バージョン情報等を持たせ、書き込み時にバージョンが最新状態かをチェックし、
最新状態のバージョンを取得していないデータのアクセスはロックされる。